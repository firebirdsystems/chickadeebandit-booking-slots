{
  "name": "chickadeebandit-booking-slots",
  "version": "1.0.0",
  "description": "Booking slots for Chickadee Bandit — type in the time slots, share one link, and people book a time",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node dev.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "license": "MIT",
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
